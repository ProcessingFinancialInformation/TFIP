<div class="form-group">
    <label for="{{name}}" class="col-sm-2 control-label">{{labelText}}</label>
    <div class="col-sm-10">
        <input type="text" ng-model="model" class="form-control" id="{{name}}" name="{{name}}" placeholder="{{labelText}}" ng-required="isRequired" ng-pattern="pattern">
    </div>
</div>
